//Using two interfaces which declare the same function, with the same signitures
//Defining a function declared in an interface

use io
use io_dup
use test4

print(str : int[]) {}
foo(i : int) : bool { return true;}